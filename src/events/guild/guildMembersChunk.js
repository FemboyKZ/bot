const { Events } = require("discord.js");

module.exports = {
  name: Events.GuildMembersChunk,
  async execute(members, guild, data, client) {
    // TODO: Implement
    // wtf does this do?
  },
};
