const { Events } = require("discord.js");

module.exports = {
  name: Events.GuildAuditLogEntryCreate,
  async execute(_auditLogEntry, _guild, _client) {
    // TODO: Implement
    // wtf does this do?
  },
};
