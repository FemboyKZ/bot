const { Events } = require("discord.js");

module.exports = {
  name: Events.EntitlementUpdate,
  async execute(oldEntitlement, newEntitlement, client) {
    // TODO: Implement
    // wtf does this do?
  },
};
