const { Events } = require("discord.js");

module.exports = {
  name: Events.MessageReactionRemoveAll,
  async execute(_message, _reactions, _client) {
    // Kinda pointless
    // TODO: Implement this event handler
  },
};
